name: runrate
description: Runrate - AI Spend Copilot
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  google_fonts: ^6.2.1
  phosphor_flutter: ^2.0.1
  get_it: ^7.7.0
  intl: ^0.19.0
  shared_preferences: ^2.2.3
  file_picker: 12.0.0-beta.7
  image_picker: ^1.2.3
  url_launcher: ^6.3.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
